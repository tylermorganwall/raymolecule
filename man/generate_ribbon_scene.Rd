% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_ribbon_scene.R
\name{generate_ribbon_scene}
\alias{generate_ribbon_scene}
\title{Build Scene (ribbon)}
\usage{
generate_ribbon_scene(
  model,
  x = 0,
  y = 0,
  z = 0,
  scale = 1,
  center = TRUE,
  model_id = NA_integer_,
  pathtrace = TRUE,
  ribbon_width = 1.6,
  ribbon_thickness = 0.25,
  cross_section_resolution = 24,
  subdivisions = 8,
  color_mode = c("chain", "uv"),
  chain_colors = NULL,
  texture = NULL,
  material = rayrender::glossy,
  material_vertex = rayvertex::material_list(type = "phong"),
  show_hetero_atoms = TRUE,
  show_hetero_bonds = TRUE,
  show_waters = FALSE,
  show_protein_atoms = FALSE,
  show_protein_bonds = FALSE,
  use_vertex_normals = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{model}{Model extracted from a PDB file with [read_pdb()].}

\item{x}{Default `0`. X offset, applied after centering.}

\item{y}{Default `0`. Y offset, applied after centering.}

\item{z}{Default `0`. Z offset, applied after centering.}

\item{scale}{Default `1`. Amount to scale the ribbon geometry.}

\item{center}{Default `TRUE`. Centers the bounding box of the model.}

\item{model_id}{Default `NA_integer_`. PDB `MODEL` identifier(s) to render.
The default `NA` renders all parsed models in the ensemble.}

\item{pathtrace}{Default `TRUE`. If `FALSE`, a `rayvertex` mesh scene is
returned. If `TRUE`, a `rayrender` scene is returned from the same mesh
data.}

\item{ribbon_width}{Default `1.6`. Width of the ribbon cross-section.}

\item{ribbon_thickness}{Default `0.25`. Thickness of the ribbon
cross-section.}

\item{cross_section_resolution}{Default `24`. Number of perimeter vertices
used to approximate the ribbon cross-section.}

\item{subdivisions}{Default `8`. Minimum number of spline samples per
residue interval. Longer backbone spans are automatically refined to a
smaller internal step size to avoid visible faceting.}

\item{color_mode}{Either `"chain"` or `"uv"`. If omitted, single-chain
proteins default to `"uv"` and multi-chain proteins default to `"chain"`.}

\item{chain_colors}{Optional named vector or named list keyed by chain ID.
Used when `color_mode = "chain"`.}

\item{texture}{Optional texture path. Used when `color_mode = "uv"`. If
omitted in UV mode, a built-in rainbow gradient is used.}

\item{material}{Default `rayrender::glossy`. Rayrender material template to
use when `pathtrace = TRUE`.}

\item{material_vertex}{Default `rayvertex::material_list(type = "phong")`.
Rayvertex material template to use when `pathtrace = FALSE`.}

\item{show_hetero_atoms}{Default `TRUE`. If `TRUE`, display non-protein
`HETATM` records as bare spheres alongside the ribbon.}

\item{show_hetero_bonds}{Default `TRUE`. If `TRUE`, display bonds between
shown hetero atoms as a ball-and-stick overlay alongside the ribbon.}

\item{show_waters}{Default `FALSE`. If `TRUE`, include water `HETATM`
records when `show_hetero_atoms = TRUE` and `show_hetero_bonds = TRUE`.}

\item{show_protein_atoms}{Default `FALSE`. If `TRUE`, display protein
`ATOM` records as small spheres alongside the ribbon.}

\item{show_protein_bonds}{Default `FALSE`. If `TRUE`, display inferred
covalent bonds between protein `ATOM` records as a thin stick overlay.}

\item{use_vertex_normals}{Default `FALSE`. If `TRUE`, attach the ribbon
mesh's swept vertex normals. If `FALSE`, raster scenes omit explicit
normals and pathtraced scenes use flat face normals.}

\item{verbose}{Default `FALSE`. If `TRUE`, report the PDB name and model
identifiers being rendered.}
}
\value{
Rayrender/rayvertex scene containing a ribbon mesh.
}
\description{
Generates a mesh-based protein ribbon from a PDB model parsed with
[read_pdb()]. Segments with contiguous peptide-plane backbone data are
rendered with a peptide-plane cartoon sweep, while incomplete backbone
segments fall back to a CA-driven centripetal Catmull-Rom ribbon. The mesh is
emitted as indexed watertight geometry with UV coordinates, and eligible
`SHEET` annotations are rendered with peptide-plane strand profiles and
tapered C-terminal arrowheads.
}
\examples{
if (file.exists("3nir.pdb")) {
  read_pdb("3nir.pdb") |>
    generate_ribbon_scene(pathtrace = FALSE) |>
    render_model()
}
}
