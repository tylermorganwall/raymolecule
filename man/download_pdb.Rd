% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_pdb.R
\name{download_pdb}
\alias{download_pdb}
\title{Download a PDB Structure from RCSB}
\usage{
download_pdb(
  protein,
  out_dir = ".",
  filename = NULL,
  overwrite = FALSE,
  max_results = 10L,
  verbose = FALSE
)
}
\arguments{
\item{protein}{Protein name or 4-character PDB ID.}

\item{out_dir}{Default `"."`. Directory where the PDB file will be written.}

\item{filename}{Default `NULL`. Optional output file name. If `NULL`, the
downloaded file is named with the matched PDB ID.}

\item{overwrite}{Default `FALSE`. Whether to overwrite an existing file.}

\item{max_results}{Default `10L`. Maximum number of RCSB search matches to
try when downloading a legacy PDB file.}

\item{verbose}{Default `FALSE`. If `TRUE`, report the matched PDB ID and
destination path.}
}
\value{
Path to the downloaded PDB file.
}
\description{
Looks up a protein name in the RCSB PDB search API and downloads the best
matching legacy PDB file. A 4-character PDB ID can also be supplied directly.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
  pdb_file = download_pdb("hemoglobin", out_dir = tempdir())
  read_pdb(pdb_file) |>
    generate_ribbon_scene(pathtrace = FALSE) |>
    render_model()
\dontshow{\}) # examplesIf}
}
